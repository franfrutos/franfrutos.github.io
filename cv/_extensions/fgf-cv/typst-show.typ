// Apply the cv() wrapper (defined in typst-template.typ) to the whole document
// body. All visible content is emitted by the generated Typst body, so no
// Pandoc metadata mapping is needed here.
#show: cv
